# Mindmap
- Guide on how to install and setup the whole data architecture from a new repo.

## Settin up dbt
 `source .venv/Scripts/activate`
- `uv add dbt-core dbt-snowflake`
    - generates pyroject.toml
- `dbt init dbt_code`