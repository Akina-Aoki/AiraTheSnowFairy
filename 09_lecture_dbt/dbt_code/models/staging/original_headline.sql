-- job_ads = database
-- staging = schema
-- data_field_job_ads = table

-- Selects the job title/headline column
SELECT
    headline

-- Reads the data from the job ads table
FROM job_ads.staging.data_field_job_ads